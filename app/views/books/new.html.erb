<h1>New book</h1>
<%= form_for(book) do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title, class: "col-xs-12" %>
  <h4>Opinion</h4>
  <%= f.text_area :body, class: "col-xs-12 new-col" %><br>
  <%= f.submit "Create Book", class: "btn btn-primary btn-block" %>
<% end %>